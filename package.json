{
  "name": "mlb-cpbl-ai-demo",
  "private": true,
  "scripts": {
    "install": "npm --prefix client i",
    "build": "npm --prefix client run build",
    "start": "node server/server.js"
  },
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
